# AGENTS.md

## コミットルール

- コミットメッセージは必ず日本語で記述する。

## 現在のプロジェクト

このプロジェクトは、雀魂のブラウザ通信を監視し、取得した手牌・捨て牌・鳴き牌から通常手の牌効率を表示する React アプリである。

現在の解析は、将来の和了確率を推定するシミュレーションではない。捨て牌後のシャンテン数と、次にシャンテン数を下げる有効牌の残り枚数を比較する。

## 実行環境と構成

実際の Vite プロジェクトは `src/` にある。ソースコードは `src/src/` に置かれている。

主要ファイル:

- `src/src/App.tsx`: アプリの状態、トラッカー接続、解析結果の計算
- `src/src/domain/tiles.ts`: 34種類の牌、表示情報、文字列配列から牌数配列への変換
- `src/src/engine/mahjong.ts`: 通常手のシャンテン数、待ち、有効牌、捨て牌候補の比較
- `src/src/tracker/client.ts`: Chrome CDP経由の雀魂WebSocket監視とイベントの状態反映
- `src/src/tracker/protocol.ts`: protobuf風データの読み取り、Base64・XOR処理
- `src/src/components/HandEditor.tsx`: 手牌、捨て牌、鳴き牌の表示と削除操作
- `src/src/components/AnalysisResults.tsx`: 捨て牌候補の順位と有効牌の残り枚数の表示
- `src/src/components/ShantenGauge.tsx`: 現在のシャンテン数と待ちの表示

利用している構成は React、TypeScript、Vite である。状態管理は React state、計算はメインスレッド上の `useMemo` で行う。

利用可能な npm script は次のとおり。

```bash
npm run dev
npm run lint
npm run build
npm run preview
```

作業ディレクトリは `src/` とする。

## 画面の現在の動作

- 起動時は `INITIAL_HAND` の13枚を表示する。
- アプリは Chrome のCDPエンドポイントと雀魂のタブへの接続を試みる。
- 雀魂から取得した自分の手牌、4座席分の捨て牌、鳴き牌を表示する。
- 手牌の牌をクリックすると削除する。自摸後の枚数になっている場合は、自分の捨て牌として座席1の捨て牌配列に追加する。
- 捨て牌と鳴き牌はクリックして削除できる。
- 「元に戻す」は直近の状態を最大10件まで戻す。「すべてリセット」は手牌、捨て牌、鳴き牌、履歴を空にする。
- 見えている牌は4座席の捨て牌と鳴き牌を合算して解析に渡す。
- おすすめの捨て牌欄では、捨て牌、順位、シャンテン数、有効牌の残り枚数を表示する。有効牌の牌種一覧は表示しない。
- シャンテン数と有効牌の残り枚数が同じ候補は同順位として表示し、同じ同順位グループを同じ色で表示する。
- 牌パレット、手入力の文字列表記、ドラ表示牌専用入力、localStorage、JSON入出力は存在しない。

## 牌の内部表現

`src/src/domain/tiles.ts` の `TILES` は次の順序で34種類を持つ。

```text
0-8   = 1m-9m
9-17  = 1p-9p
18-26 = 1s-9s
27-33 = 1z-7z
```

エンジンの `Counts` は長さ34の通常の `number[]` であり、`Uint8Array` ではない。画面やトラッカーでは `1m` のような文字列IDを使い、`toTileCounts` で牌数配列に変換する。

## 解析ロジック

実装は `src/src/engine/mahjong.ts` に集約されている。

### シャンテン数

`standardShanten(input, fixedMelds)` は、4面子1雀頭の通常手だけを評価する。

- 面子、ターツ、雀頭の組み合わせを再帰的に探索する。
- 順子は萬子・筒子・索子だけで作る。
- 字牌は刻子または雀頭として扱い、順子・連続ターツには使わない。
- `fixedMelds` は完成済みのポン・チーなどの鳴き面子数で、探索開始時から完成面子として数える。
- 七対子、国士無双、役の有無、リーチ可否、点数は評価しない。

### 捨て牌候補

`analyzeDiscards(hand, visible, fixedMelds)` は、自摸後の手牌を前提にする。

解析対象の手牌枚数は次の式で決まる。

```text
14 - fixedMelds * 3
```

したがって、ポン・チーがない場合は14枚、1面子なら11枚、2面子なら8枚である。打牌後はそれぞれ13枚、10枚、7枚になる。

処理順:

1. 手牌に存在する牌種を1つずつ捨て牌候補にする。
2. 候補を1枚除き、`standardShanten` で捨て牌後のシャンテン数を計算する。
3. 34種類の牌を1種類ずつ加え、シャンテン数が下がる牌だけを有効牌にする。
4. 見えている牌を除いた残り枚数を、有効牌種ごとに合計する。
5. 次の順に結果をソートする。

   1. シャンテン数の昇順
   2. 有効牌の残り枚数の降順
   3. 有効牌の種類数の降順
   4. 牌番号の昇順

同じ牌を複数枚持っている場合、同じ牌種を捨てた結果は同一なので、物理的な14枚分ではなく牌種単位で候補を作る。

有効牌の残り枚数は次で計算する。

```text
4 - 捨て牌後の手牌の牌種枚数 - 既存の visible の牌種枚数 - 今回仮に捨てる牌
```

候補の捨て牌は仮の河に追加してから、次のツモ候補と残り枚数の計算から除外する。`visible` は既存の河と鳴き牌を合算した配列である。入力配列の不正な値をエンジンが検証・修正することはない。

### 現在のシャンテン数と待ち

- `currentShanten` は、自摸後の手牌枚数なら全捨て牌候補の最小シャンテン数を返す。それ以外の枚数なら、その状態をそのまま `standardShanten` に渡す。
- `currentWaits` は、打牌後の通常手が0シャンテンの場合、和了形になる牌を待ちとして列挙する。
- 14枚相当の自摸後状態では、最上位の捨て牌候補が0シャンテンの場合に、その候補の有効牌を表示する。
- 待ちと有効牌はいずれも通常手だけが対象である。

## 鳴きの扱い

`App.tsx` はトラッカーが保持する `ownSeat` を使い、自分の鳴き面子数を `fixedMelds` としてエンジンに渡す。

- ポン・チーを複数回行った場合も、鳴き面子の個数分だけ固定面子として扱う。
- 鳴き面子の牌そのものは、全座席の鳴き牌として `visible` に入り、残り枚数から除外される。
- トラッカーは `ActionChiPengGang` を鳴きとして記録する。
- トラッカーはカンイベントも状態には記録するが、解析側の手牌枚数調整は1面子あたり3枚固定である。そのため、カンを含む局面の解析は正確ではない。
- 門前かどうか、リーチ可能かどうか、鳴いたことによる役の有無は判定しない。

## トラッカーの現在の対応範囲

`MahjongSoulTracker` は次のイベントを処理する。

- `ActionNewRound`: 手牌と局の状態を初期化
- `ActionDealTile`: 自分の手牌に自摸牌を追加
- `ActionDiscardTile`: 座席の河に牌を追加し、自分の手牌から捨て牌を削除
- `ActionChiPengGang`: 河から鳴かれた牌を削除し、鳴き面子を追加
- `ActionAnGangAddGang`: 暗槓・加槓・明槓の状態を記録
- `authGame` の送受信: `account_id` と `seat_list` から自分の座席を確定
- `syncGame` / `enterGame`: `GameRestore.actions` を再生して、途中接続時の局状態を復元

プロトコルの解析に失敗したフレームや関係のないフレームは無視する。

`ActionNewRound` の field 2 は親の座席（`ju`）であり、常に自分の座席ではない。通常は `authGame` の `account_id` と `seat_list` から自分の座席を確定する。途中接続時は、配牌14枚なら `ju` を自分の座席とみなし、配牌13枚なら実牌を含む自摸イベントの座席を補助的に使う。

## 変更時の注意

- 計算ロジックを変更するときは `src/src/engine/mahjong.ts` を変更し、Reactコンポーネントに牌効率計算を追加しない。
- `Counts` の牌番号順と34種類の対応を変更しない。変更する場合は `TILES`、トラッカーの牌変換、エンジンを同時に確認する。
- 捨て牌のランキング基準を変更する場合は、`analyzeDiscards` の `sort` と `AnalysisResults` の表示説明を同時に確認する。
- 鳴き面子数を変更する場合は、`App.tsx` の `fixedMelds`、解析対象の手牌枚数、トラッカーの `melds` 更新を同時に確認する。
- UIを変更した場合は `npm run lint` と `npm run build` を実行する。
- エンジンを変更した場合も、最低限 `npm run lint` と `npm run build` を実行する。現在、package.json に自動テスト用の script はない。
