# AGENTS.md

## コミットルール

- コミットメッセージは必ず日本語で記述する。例外は認めない。

## プロジェクト概要

ユーザーが手動で次の項目を選択できる、ブラウザベースの麻雀解析アプリを構築する。

1. 現在の手牌
2. 河、鳴き、ドラ表示牌を含む見えている牌
3. 評価するツモ回数または手番数

アプリは、選択された期間内にテンパイまたは和了する確率が最も高くなる捨て牌を継続的に再計算する。

初回リリースでは、高度な相手モデルよりも正確性、速度、手動入力のしやすさを優先する。

---

## 主目的

14枚の手牌と見えている牌から、合法なすべての捨て牌を確率順に並べる。

既定のランキング指標は次のとおり。

> 今後 N 回の自摸で和了する確率

補助指標として次も表示する。

- 捨て牌後の現在のシャンテン数
- 有効牌の種類
- 有効牌の枚数
- 今後 N 回の自摸でテンパイする確率
- 今後 N 回の自摸で和了する確率
- 良い待ちで終わる確率
- 成功したシミュレーションにおける平均和了自摸

ユーザーが手牌や見えている牌を変更すると、結果が自動更新されなければならない。

---

## 初期スコープ

初版は次の前提で実装する。

- 日本式リーチ麻雀
- 4人用の牌セット
- 門前手のみ
- ユーザーによる鳴きなし
- カンなし
- 赤牌なし
- 点数・期待得点なし
- 相手の手牌推定なし
- 守備・放銃リスク計算なし
- 未知の牌は、すべての見えていない牌から一様に分布する
- 見えている牌は未知牌のプールから正確に除外する
- 役牌を含む通常手、七対子、国士無双に対応する
- ユーザーは自摸後に必ず捨て、以後は自摸シミュレーションを行う
- 初版では相手の自摸を無視してよい。後から明示的に有効化する場合のみ、未知牌をランダムに除外する形でモデル化する

初回リリースに推測的なAIや画像認識を追加しない。

---

## 推奨技術

推奨構成:

- フロントエンド: React + TypeScript
- ビルドツール: Vite
- スタイリング: シンプルなCSS ModulesまたはTailwind CSS
- 計算エンジン: Web Worker上のTypeScript
- テスト: Vitest
- エンドツーエンドテスト: Playwright
- 状態管理: React stateまたはZustand
- 永続化: localStorage

初版にPythonバックエンドは必要ない。性能測定でサーバーが必要だと判明しない限り、アプリは完全にクライアント側で動作させる。

別の構成を使う場合も、UI、牌モデル、手牌評価、シミュレーションエンジンの分離を維持する。

---

## ユーザーインターフェース

### メインレイアウト

次の4つの主要領域を作る。

1. 牌パレット
2. 現在の手牌
3. 見えている牌
4. 解析結果

### 牌パレット

34種類の牌を種類順に表示する。

- 萬子: 1m〜9m
- 筒子: 1p〜9p
- 索子: 1s〜9s
- 字牌: 東、南、西、北、白、發、中

牌をクリックすると、現在アクティブな入力先に追加する。

- 手牌
- 見えている牌

アクティブな入力先は視覚的に明確にする。

手牌と見えている牌を合計した枚数が4枚を超える入力は、無効化または拒否する。

### 現在の手牌

要件:

- 13枚または14枚に対応する
- 種類と数字で自動ソートする
- 牌をクリックすると1枚削除する
- 不正な牌数を明確に示す
- 手牌の短縮テキスト表記を表示する
- クリアと元に戻すを提供する
- 任意の補助入力として直接テキスト入力に対応する

受け付ける表記例:

```text
29m2366p23344679s
123m456p789s11z
```

字牌の表記:

```text
1z = 東
2z = 南
3z = 西
4z = 北
5z = 白
6z = 發
7z = 中
```

### 見えている牌

見えている牌の領域は、各プレイヤーの河を再構成するものではなく、合計枚数を扱う。

要件:

- 牌をクリックすると見えている牌に1枚追加する
- 既存の牌をクリックすると見えている牌から1枚削除する
- 同じ牌が複数ある場合は枚数を表示する
- 必要に応じて次のグループに分けられるようにする
  - 捨て牌
  - 副露
  - ドラ表示牌
- 初版の計算エンジンが必要とするのは牌ごとの合計枚数だけとする
- 元に戻すとクリアを提供する

### 解析コントロール

次を含める。

- 評価する今後の自摸回数
- シミュレーション回数
- ランキング指標
- 詳細設定に隠した乱数シード
- 高速計算または詳細計算モード

推奨初期値:

```text
今後の自摸回数: 8
高速シミュレーション: 5,000
詳細シミュレーション: 50,000
ランキング指標: 和了確率
```

### 結果

捨て牌候補ごとに1行または1カードを表示する。

各結果には次を含める。

- 捨て牌
- 順位
- 捨て牌後のシャンテン数
- 有効牌
- 有効牌の枚数
- テンパイ確率
- 和了確率
- 良い待ちの確率
- 平均和了自摸
- シミュレーションの不確実性または信頼区間

選択されたランキング指標でソートする。

最良の候補を強調表示する。

差がシミュレーション誤差の範囲内である場合、候補が決定的に優れているとは主張しない。「統計的に同程度」と表示する。

---

## 中核ドメインモデル

34種類の牌を0〜33の整数で表す。

推奨マッピング:

```text
0-8   = 1m-9m
9-17  = 1p-9p
18-26 = 1s-9s
27    = 東
28    = 南
29    = 西
30    = 北
31    = 白
32    = 發
33    = 中
```

固定長の枚数配列を使う。

```ts
type TileCounts = Uint8Array; // length 34
```

中核型:

```ts
type TileId = number;

interface AnalysisInput {
  hand: TileCounts;
  visible: TileCounts;
  futureDraws: number;
  simulations: number;
  seed?: number;
}

interface DiscardResult {
  discard: TileId;
  shanten: number;
  effectiveTiles: TileId[];
  effectiveTileCount: number;
  tenpaiRate: number;
  winRate: number;
  goodWaitRate: number;
  averageWinningDraw: number | null;
  standardError: number;
  simulations: number;
}
```

内部表現に表示用の文字列を使わない。

---

## バリデーション規則

解析前に次を確認する。

- 捨て牌解析では手牌がちょうど14枚である
- 各牌の枚数が0〜4の範囲である
- 手牌と見えている牌を合計した各牌の枚数が4枚以下である
- 手牌と見えている牌の合計が136枚以下である
- 今後の自摸回数が正の整数である
- シミュレーション回数が安全に設定された範囲内である

バリデーションエラーはUIに表示する。不正な入力を黙って修正しない。

---

## 手牌評価

次を実装し、テストする。

- 通常手のシャンテン数
- 七対子のシャンテン数
- 国士無双のシャンテン数
- 対応する手の種類の最小値としての総合シャンテン数
- 和了形の判定
- テンパイ判定
- 待ち牌の列挙
- 有効牌の列挙

通常手の評価は次に対応する。

- 完成した面子
- 未完成の順子形
- 雀頭
- 重複する形
- 字牌の制約
- 複数の分解候補

微細な最適化よりも正確性を優先する。

必要に応じて、符号化した牌数をキーにメモ化する。

---

## 有効牌

13枚の状態において、有効牌とは、加えることでシャンテン数が下がる、かつ見えていない牌である。

有効牌の枚数は、すべての有効牌種について残り枚数を合計した値とする。

残り枚数:

```text
4 - 現在のシミュレーション手牌の枚数 - 見えている牌 - シミュレーション中に除外された牌
```

5枚目の牌を決して数えない。

捨て牌後の現在の有効牌を表示する際は、ユーザーが入力した正確な見えている牌情報を使う。

---

## シミュレーションモデル

N回の自摸でテンパイまたは和了する確率にはモンテカルロ法を使う。

各捨て牌候補について:

1. 14枚の手牌から候補牌を1枚取り除く
2. 136枚すべての牌から未知牌のプールを構築する
3. 残った13枚の手牌を取り除く
4. 見えている牌をすべて取り除く
5. シード付き疑似乱数生成器でプールをシャッフルまたはサンプリングする
6. 最大N回の今後の自摸をシミュレーションする
7. 各自摸後に次を行う
   - 手牌が和了形か確認する
   - 和了形なら和了を記録して停止する
   - それ以外の場合は、シミュレーション方針で捨て牌を選ぶ
8. 途中でテンパイしたか記録する
9. 最終的なテンパイ待ちが良い待ちか記録する
10. 結果を集計する

シミュレーションの最内ループで大きな配列を確保しない。

---

## シミュレーション時の捨て牌方針

今後の捨て牌方針は結果に大きく影響する。ランダムな捨て牌方針は使わない。

初期方針:

1. シャンテン数を最小化する
2. シャンテン数が同じなら有効牌の枚数を最大化する
3. それでも同じなら良い待ちの可能性を最大化する
4. それでも同じなら中央牌と連続性のある牌を残す
5. 再現性のため、牌の決定的な順序で同点を解消する

方針は次のインターフェースの背後に分離する。

```ts
interface DiscardPolicy {
  chooseDiscard(hand14: TileCounts, visibleAndRemoved: TileCounts): TileId;
}
```

これにより、後から次の方針に置き換えられる。

- 厳密な動的計画法
- ビームサーチ
- より強いヒューリスティック評価器
- 期待得点
- 相手を考慮した打牌

表示される確率は、この継続打牌方針に依存することを文書化する。

---

## 良い待ちの定義

初版では、残りの和了牌の総数が4枚以上のテンパイ待ちを良い待ちと分類する。

可能な場合は、正確な待ち牌と残り枚数も公開する。

将来、より詳細な分類で次を区別できるよう、このルールは設定可能にする。

- 両面
- 双碰
- 嵌張
- 辺張
- 単騎
- 多面待ち

牌の種類数だけで待ちの良さを推測しない。

---

## 厳密計算と近似計算

次は厳密に計算する。

- シャンテン数
- 有効牌の種類
- 有効牌の枚数
- 現在の待ち
- 見えている牌を考慮した残り枚数

次はモンテカルロ近似で計算する。

- N回の自摸でテンパイする確率
- N回の自摸で和了する確率
- 今後良い待ちになる確率
- 平均和了自摸

シミュレーション値が推定値であることを表示する。

シミュレーション回数nにおける二項分布の割合pについて、標準誤差を次で推定する。

```text
sqrt(p * (1 - p) / n)
```

単純な95%区間は、おおよそ次で表示できる。

```text
p ± 1.96 * standard error
```

表示する区間は0%〜100%に収める。

---

## 応答性とバックグラウンド計算

シミュレーションはWeb Workerで実行する。

必須動作:

- 入力変更でデバウンスされた計算を開始する
- 短い待機後、高速モードを自動開始する
- 詳細モードは高速結果の表示後、または要求されたときだけ開始する
- 新しい入力で古い計算をキャンセルする
- WorkerのメッセージにリクエストIDを含める
- 破棄されたリクエストの遅延結果を無視する
- 詳細計算の進捗を表示する
- 計算中もUIの応答性を維持する

推奨デバウンス時間:

```text
150-300 ms
```

次の値から安定したハッシュを作り、結果をキャッシュする。

- 手牌
- 見えている牌
- 今後の自摸回数
- シミュレーション回数
- 方針のバージョン
- シード

---

## 再現性

決定論的なシード付き疑似乱数生成器を実装する。

要件:

- 同じ入力とシードから同じ結果になる
- 捨て牌候補ごとに独立した決定論的ストリームを使う
- テストが`Math.random()`に依存しない
- エクスポートした解析データにシードを保存する

---

## 性能目標

一般的なデスクトップブラウザで:

- 厳密なシャンテン数と有効牌の結果: 50ms未満
- 全捨て牌候補の高速解析: 1秒未満を目標とする
- 詳細解析: 5秒未満を目標とする
- 計算中もUI操作が応答する

最適化の前に測定する。

主な最適化候補:

- 牌数のコンパクトな符号化
- メモ化したシャンテン評価
- 再利用可能な型付き配列
- 牌の関係の事前計算
- シミュレーションループ内のオブジェクト生成回避
- 必要に応じたWorkerの並列化

同値性を示すテストなしに、正確性を犠牲にした最適化をしない。

---

## プロジェクト構成

推奨構成:

```text
src/
  app/
    App.tsx
  components/
    Tile.tsx
    TilePalette.tsx
    HandEditor.tsx
    VisibleTilesEditor.tsx
    AnalysisControls.tsx
    ResultsTable.tsx
  domain/
    tiles.ts
    notation.ts
    validation.ts
    shanten.ts
    waits.ts
    effectiveTiles.ts
    handTypes.ts
  engine/
    analyze.ts
    simulate.ts
    discardPolicy.ts
    rng.ts
    cache.ts
  workers/
    analysis.worker.ts
    protocol.ts
  state/
    useMahjongStore.ts
  styles/
  tests/
```

UIコードを計算エンジンに入れない。

---

---

## アクセシビリティ

- すべての牌ボタンにアクセシブルなラベルを付ける
- キーボード利用者が牌を追加・削除できるようにする
- 結果の順位を色だけで伝えない
- 視認できるフォーカス状態を提供する
- 十分なコントラストを確保する
- 結果表をスクリーンリーダーに対応させる
- 視覚効果を減らす設定に対応する

ラベル例:

```text
3索を手牌に追加
見えている牌から筒子の6を1枚削除
九萬を捨てる
```

---

## データ永続化

現在のセッションをlocalStorageに保存する。

- 手牌
- 見えている牌
- 今後の自摸回数
- 計算モード
- ランキング指標
- 詳細設定

すべてリセットする操作を用意する。

任意でJSONのインポートとエクスポートにも対応する。

エクスポート形式の例:

```json
{
  "version": 1,
  "hand": "29m2366p23344679s",
  "visible": "144p5888s",
  "futureDraws": 8,
  "simulations": 50000,
  "seed": 123456
}
```

インポートしたデータは、使用前に検証する。

---

## エラー処理

次のエラーを明確に表示する。

- 不正な手牌枚数
- 5枚目の牌
- 不正な表記
- 手牌と見えている牌の組み合わせが不可能
- Workerのエラー
- 計算のキャンセル
- 過大なシミュレーション要求

不正な入力でページをクラッシュさせない。

技術的な詳細は開発環境でのみコンソールに記録する。

---

## プロダクト上の表現

確率に関する表現は慎重に扱う。

推奨:

- 「推定和了確率」
- 「推定テンパイ確率」
- 「現在の設定で最良」
- 「統計的に同程度」
- 「見えていない牌が一様に出現すると仮定」

避ける表現:

- 「正しい捨て牌」
- 「絶対に最良」
- 「完全な打牌」
- モンテカルロ結果に対する「正確な和了確率」

次の注意書きを簡潔に表示する。

> この推定は、見えていない牌が一様に分布すると仮定しており、相手の手牌、鳴き、守備、点数状況はモデル化していません。

---

## バージョン1の完了条件

次を満たしたとき、バージョン1を完了とする。

- ユーザーが有効な14枚の手牌を選択できる
- ユーザーが見えている牌を合計枚数で入力できる
- 5枚目の牌が防止される
- 合法なすべての捨て牌候補が解析される
- 厳密なシャンテン数と有効牌の枚数が表示される
- モンテカルロによるテンパイ確率と和了確率が表示される
- 入力変更後に結果が自動更新される
- 計算がメインUIスレッド外で実行される
- 固定シードで再現可能な結果になる
- 中核の評価器とシミュレーションロジックに自動テストがある
- 現行のデスクトップおよびモバイルブラウザで動作する
- 静的サイトとしてデプロイできる

---

## 将来の作業

バージョン1が安定するまで、次を実装しない。

- 赤牌
- 門前以外の手
- 鳴きとカン
- ドラと点数期待値
- リーチ判断
- 相手の捨て牌モデル
- 守備と放銃確率
- 相手の手牌推定
- 山の構成推定
- 厳密な動的計画法
- ビームサーチ
- 対局ポイントまたは順位期待値
- 画像認識
- オンライン麻雀クライアントからのライブ取得
- 複数プレイヤーの河の再構成

牌モデルやUIを書き直さずに追加できるよう、インターフェースを設計する。

---

## エージェントの作業手順

変更を行うとき:

1. このファイルを読み、既存のアーキテクチャを確認する
2. 牌数の不変条件を維持する
3. 中核の確率ロジックを変更する前にテストを追加または更新する
4. 計算ロジックをReactコンポーネントから分離する
5. フォーマット、型チェック、単体テスト、エンドツーエンドテストを実行する
6. シミュレーションコードを変更したら性能を測定する
7. 仮定と追加した近似を文書化する
8. ランキング指標やシミュレーション方針を黙って変更しない
9. コミットを小さく分け、レビューしやすくする
10. コミットメッセージはすべて日本語で記述する

作業完了を宣言する前に、次を実行する。

```bash
npm run lint
npm run typecheck
npm run test
npm run build
```

UIまたは操作フローを変更した場合は、エンドツーエンドテストも実行する。

```bash
npm run test:e2e
```

---

## 絶対に守る不変条件

- 手牌と見えている牌を合わせて、同じ牌が5枚以上存在してはならない
- シミュレーションで未知牌のプールにない牌を自摸してはならない
- 厳密な指標がランダムサンプリングに依存してはならない
- 古いWorker結果が新しい結果を上書きしてはならない
- シード付きシミュレーションは再現可能でなければならない
- シミュレーション確率が推定値であることをユーザーに伝えなければならない
- 中核の麻雀ロジックはUIから独立してテストしなければならない
